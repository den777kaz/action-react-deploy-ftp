on:
  push:
    branches: [master]
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v2.3.2

      - name: Use Node.js 12
        uses: actions/setup-node@v2-beta
        with:
          node-version: '12'

      - name: 🔨 Build Project
        run: |
          npm install
          npm run build

      - name: 📂 Sync files
        uses: sand4rt/ftp-deployer@v1.1
        with:
          host: ftp.hostde30.fornex.host
          username: denis@den-kaz.com
          password: ${{ secrets.password }}
          local_folder: build
